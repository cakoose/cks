package cks.type.model;

/**
 * A type description.  This is what goes on the right-hand side of a
 * type definition.
 *
 *    def MyType = [TDesc]
 *
 * Can be a compound type description (record/variant) or a type
 * expression, in which case the type definition is an alias.
 */
public abstract class TDesc
{
}
